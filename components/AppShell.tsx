"use client";
import Link from "next/link";import {signOut} from "next-auth/react";import type {User} from "@/lib/types";
export function AppShell({user,children}:{user:User;children:React.ReactNode}){const roles=user.roles;return <div className="shell"><aside className="side"><Link href="/portal"><img className="logo" src="/logo-dark.png" alt="Sankari Holding"/></Link><nav className="nav"><Link href="/portal">My requests</Link>{(roles.includes("agent")||roles.includes("admin"))&&<Link href="/admin">Team queue</Link>}{(roles.includes("board")||roles.includes("admin"))&&<Link href="/dashboard/kpi">KPI dashboard</Link>}{(roles.includes("dev")||roles.includes("admin"))&&<Link href="/dashboard/jira">Jira dashboard</Link>}{roles.includes("admin")&&<Link href="/settings">Settings</Link>}</nav><div className="side-foot"><div className="identity"><strong>{user.name}</strong><small>{user.email}</small><br/><small>{roles.join(" · ")}</small></div><button className="logout" onClick={()=>signOut({callbackUrl:"/login"})}>Sign out</button></div></aside><div className="main"><header className="top"><span className="eyebrow">Unified systems platform</span><span className="health">Live</span></header><main className="content">{children}</main></div></div>}
